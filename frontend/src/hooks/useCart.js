// Custom hook - shortcut to consume CartContext
// Returns: { items, totalItems, totalPrice, addItem, removeItem, updateQuantity, clearCart }
// Usage: const { items, addItem } = useCart();

export { useCart } from '../context/CartContext'
