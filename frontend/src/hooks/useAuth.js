// Custom hook - shortcut to consume AuthContext
// Returns: { user, token, isAuthenticated, loading, login, logout, register }
// Usage: const { user, login } = useAuth();

export { useAuth } from '../context/AuthContext'
